/*
  double-ened queue 
*/
const deque = [];

// push element from rear end
const newLength = deque.push(1);
// pop element from front end
const fistVal = deque.shift();

// push element from front end
const newLength = deque.unshift(val);
// pop element from rear end
const  lastVal = deque.pop();